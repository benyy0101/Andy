'use client'
import tw from "tailwind-styled-components";

export const Calendar_box: React.FC = () => {
    return (
        <Wrapper>
            달력
        </Wrapper>
    )
};

const Wrapper = tw.div`
flex
`